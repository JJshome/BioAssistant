# Integration Tests

This directory contains integration tests that verify interactions between different services or components of the BioTech AI Platform.

- These tests typically target service APIs directly.
- Frameworks like Pytest (for Python services) or Jest/Mocha (for Node.js services) might be used, potentially with tools like `requests` or `axios`.
- Docker Compose might be used to spin up dependent services for testing.
